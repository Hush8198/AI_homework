{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "read_excel_rows",
        "description": "使用Python的openpyxl库打开指定的Excel文件，并读取前三行的数据",
        "parameters": {
          "type": "object",
          "properties": {
            "file_path": {
              "type": "string",
              "description": "Excel文件的路径，例如E:/test/hsh.xlsx"
            }
          },
          "required": [
            "file_path"
          ]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "install_openpyxl",
        "description": "Installs the openpyxl library using pip, which is a Python library to read/write Excel 2010 xlsx/xlsm/xltx/xltm files.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "calculate_sum_top3_rows",
        "description": "计算Excel文件中前三行每列的和，并将结果存储到第四行",
        "parameters": {
          "type": "object",
          "properties": {
            "file_path": {
              "type": "string",
              "description": "Excel文件的路径，例如E:/test/hsh.xlsx"
            },
            "sheet_name": {
              "type": "string",
              "description": "要操作的工作表名称，如果为空，则默认操作第一个工作表"
            }
          },
          "required": [
            "file_path"
          ]
        }
      }
    }
  ]
}